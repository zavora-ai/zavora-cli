# Archived documentation

These documents describe superseded v1-era state and are kept for provenance
only. They are **not** current and are excluded from the documentation harness
(`scripts/check_docs.sh`), so their examples are not guaranteed to work.

For current documentation, start from the root `README.md`.
