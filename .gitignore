/target
.env
.zavora/
.cargo/config.toml
evals/reports/latest.json
.DS_Store
*.swp
*.swo
*.bak
.idea/
.vscode/
go/
# `.skills/` is a user-local discovery root scaffolded on first run by
# src/onboarding.rs. It is not bundled product content; the skills that ship
# with the repository live in `.agents/skills/<name>/SKILL.md`.
.skills/
# Local scratch workspaces, never product content.
test/
test-projects/
hello_world/
